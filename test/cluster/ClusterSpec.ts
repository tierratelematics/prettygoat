describe("Given a set of nodes", () => {
    context("when a node starts", () => {
        it("should run the projections that match", () => {

        });
    });

    context("when the nodes ring is changed", () => {
        it("should redistribute the projections");
    });
});