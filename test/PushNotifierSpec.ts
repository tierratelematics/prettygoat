import expect = require('expect.js');
import sinon = require('sinon');

describe("PushNotifier, given an observable and an endpoint", () => {

    context("when they are registered together", () => {
        it("should create an endpoint to retrieve the latest model emitted by the observable");
    });

    context("when a new event is triggered by the observable", () => {
        it("should emit a notification on the corresponding endpoint");
    });
});